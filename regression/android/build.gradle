plugins {
    id 'com.android.library'
    id 'de.mannodermaus.android-junit5'
}

android {
    namespace 'com.github.neboskreb.red.and.blue.android'

    defaultConfig {
        minSdkVersion 34
        targetSdkVersion 35
        compileSdk = 35

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    androidTestImplementation project(":red-and-blue")
    androidTestImplementation platform('org.junit:junit-bom:6.0.2')
    androidTestImplementation 'org.junit.jupiter:junit-jupiter-api'
    androidTestImplementation 'org.assertj:assertj-core:3.27.6'
}
